# Copia este archivo como .env para usar PostgreSQL local (docker-compose)
# Si no defines DATABASE_URL, la app usa SQLite automáticamente (./lis_equipos.db)
DATABASE_URL=postgresql://lis:lis_password@localhost:5432/lis_equipos
