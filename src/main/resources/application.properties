spring.application.name=reservas-lis

# Conexion a la base de datos.
# Se leen variables de entorno (DB_URL, DB_USERNAME, DB_PASSWORD). 
# Si no existen, se usa por defecto la instancia de pruebas en Neon DB para facilitar la revision.
spring.datasource.url=${DB_URL:jdbc:postgresql://ep-bold-violet-aydb4m8v-pooler.c-5.us-east-2.aws.neon.tech/neondb?sslmode=require&channel_binding=require}
spring.datasource.username=${DB_USERNAME:neondb_owner}
spring.datasource.password=${DB_PASSWORD:npg_kQR5VZEb8CsK}

# Configuracion JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Ruta de Swagger
springdoc.swagger-ui.path=/swagger-ui.html
