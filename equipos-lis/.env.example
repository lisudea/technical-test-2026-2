# URL base de la REST API del backend (rama 1022003345-reto2).
# Copia este archivo como .env.local y ajusta el valor si el backend corre en otro host/puerto.
# Si no existe, se usa por defecto http://localhost:8080/api
VITE_API_URL=http://localhost:8080/api
