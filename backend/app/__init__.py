"""Paquete principal de la aplicación.

Marca la carpeta ``app/`` como un paquete de Python, de modo que el resto del
proyecto (Alembic, las pruebas) pueda importar su contenido con rutas claras
como ``from app.models import Equipo``.
"""
