package udea.lis.equipos_reservas_api.model;

// Esta clase representa las categorías de los equipos que se pueden reservar. Es un enum, lo que significa que tiene un conjunto
// fijo de valores posibles. Es usada en la clase Equipo para indicar a qué categoría pertenece un equipo.
public enum CategoriaEquipo {
    MICROCONTROLADORES,
    VR,
    REDES
}
